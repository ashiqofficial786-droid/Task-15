[pytest]
; Always emit a self-contained HTML report (Note #2 in the task: mandatory).
addopts = --html=reports/report.html --self-contained-html -v
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
